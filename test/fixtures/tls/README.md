# TLS test fixture

`localhost-cert.pem` and `localhost-key.pem` are a self-signed localhost-only test certificate and
private key. They are public fixtures with no trust or use outside the HTTPS integration test.
